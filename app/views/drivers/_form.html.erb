<%= form_for(@driver) do |f| %>
  <% if @driver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@driver.errors.count, "error") %> prohibited this driver from being saved:</h2>

      <ul>
      <% @driver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
        <%= f.label :name , class: "sign-lable" %>
        <%= f.text_field :name, autofocus: true, class: "form-control sign-input" %>
      </div>
      <div class="form-group">
        <%= f.label :age,class: "sign-lable" %>
        <%= f.number_field :age, autocomplete: "off" ,class: "form-control sign-input" %>
      </div>
<% end %>
